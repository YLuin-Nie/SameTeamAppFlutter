name: same_team_flutter
description: SameTeamAppFlutter is a cross-platform productivity and parenting tool that connects to a secure ASP.NET Core Web API backend hosted on Azure. Parents can create teams, assign chores and rewards, and monitor progress, while children can complete chores and redeem points — all through a seamless mobile, web, or desktop experience.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.4.0
  shared_preferences: ^2.2.2
  cupertino_icons: ^1.0.2
  intl: ^0.20.2
  jwt_decoder: ^2.0.1
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  provider: ^6.1.5
  flutter_launcher_icons: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.6
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  flutter_native_splash: ^2.4.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/logodark.png

flutter_native_splash:
  color: "#ffffff"
  image: assets/app_icon.png
  android: true
  ios: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/app_icon.png"